// import { CollageImage } from "../lib/definitions";
// import { collageImages } from "../lib/data";

export default function Test() {
  return (
    <div className="w-1/2 h-auto m-auto">
      {/* <svg className="w-full h-full" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1920 1283">
        {collageImages.map((img: CollageImage) => {
          return (
            <image
              key={img.href}
              href={img.href}
              x={img.x}
              y={img.y}
              width={img.width}
              height={img.height}
              className="appear-in origin-center hover:transform hover:rotate-3"
            />
          );
        })}
      </svg> */}
    </div>
  );
}
